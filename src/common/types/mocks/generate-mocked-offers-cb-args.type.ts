import { GenerateMockedOfferCbArgs } from '~/common/types/mocks/generate-mocked-offer-cb-args.type';

type GenerateMockedOffersCbArgs = {
  count: number;
} & GenerateMockedOfferCbArgs;

export { GenerateMockedOffersCbArgs };
