enum TableName {
  CATEGORIES = `categories`,
  OFFER_TYPES = `offer_types`,
  USERS = `users`,
  COMMENTS = `comments`,
  OFFERS = `offers`,
  OFFERS_CATEGORIES = `offers_categories`,
  SESSIONS = `sessions`,
}

export { TableName };
